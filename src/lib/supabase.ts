/**
 * GERAX TELECOM - CLIENTE UNIFICADO
 * Redireciona para o serviço centralizado para manter compatibilidade
 * com componentes antigos enquanto migramos para a service layer.
 */
export { supabase } from '../services/supabase/client';
